#!/usr/bin/env ruby
require_relative "../lib/greeting.rb"

# code your CLI here!
puts "Hi! I'm Hal, what's yout name?"
def greeting(name)
  puts "Hello #{name.strip}. It's nice to meet you."
end
name = gets.strip
greeting(name)
